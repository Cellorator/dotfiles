#!/bin/sh

picom -b &

/usr/lib/xfce-polkit/xfce-polkit &
dunst &
udiskie --tray &
blueman-applet &
nm-applet --no-agent &

gwe --hide-window &
easyeffects --gapplication-service &

disable-mouse-acceleration
set-key-repeat-rate
